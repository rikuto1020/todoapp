= form_with model: @category, local: true do |f|
  = f.text_field :name, class: "SettingGroupForm__input", placeholder: "カテゴリー名を入力してください"
  = f.collection_check_boxes :user_ids, User.all, :id, :name
  = f.submit "カテゴリー作成", class: "SettingGroupForm__button"
